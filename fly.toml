# Bot dùng long polling nên không mở HTTP service công khai.
app = 'sober-bot'
primary_region = 'nrt'

[env]
  TZ = "Asia/Ho_Chi_Minh"

[build]

[processes]
  app = "python main.py"

[deploy]
  strategy = "immediate"

[[vm]]
  memory = '256mb'
  cpu_kind = 'shared'
  cpus = 1
